#!/bin/bash
echo "Bash version ${BASH_VERSION}..."
for i in {10..100}
    do        
    if (( $i % 5 == 0 ))           # no need for brackets
    then
        myNAME='_Qubits'
        myNAME=$i$myNAME
        mkdir $myNAME         
    fi
done

